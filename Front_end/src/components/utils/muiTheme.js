import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
      primary: {
        light: '#33c2c3',
        main: '#00b3b4',
        dark: '#007d7d',
        contrastText: '#fff',
      }
    },
});

export default theme 